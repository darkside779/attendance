// Configuration for different environments
window.APP_CONFIG = {
  // This will be automatically set based on the hostname
  API_BASE_URL: 'auto', // 'auto' means it will detect the current host
  
  // You can override this for specific deployments:
  // API_BASE_URL: 'http://192.168.1.196:8001/api/v1'
};
